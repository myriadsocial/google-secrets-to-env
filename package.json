{
  "name": "google-secrets-to-env",
  "version": "1.0.0",
  "description": "A Node.js utility for fetching secrets from Google Secret Manager and generating a .env file with those secrets. Designed to streamline development and deployment workflows by automating the process of secrets management. The utility uses the Google Cloud SDK for authentication and accesses the Secret Manager API to retrieve secret values, which are then written to a local .env file in KEY=value format.",
  "main": "src/index.js",
  "scripts": {
    "start": "npx ts-node --transpile-only src/index.ts",
    "build": "tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/secret-manager": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.3"
  }
}
